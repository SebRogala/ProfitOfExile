# Placeholder: will be replaced by SvelteKit dev server (npm run dev -- --host)
# when the frontend scaffold is introduced.
FROM node:22-alpine

RUN npm install -g serve@14

WORKDIR /app

EXPOSE 5173

CMD ["serve", "-s", ".", "-l", "5173"]
